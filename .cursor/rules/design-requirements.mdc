---
description: 改代码前对照 Design/Requiment 需求文档；BUG 与需求类建议须回写文档
alwaysApply: true
---

# 需求文档（Design/Requiment）

主文档：**`Design/Requiment/FancyWM-主要需求与约定.md`**

## 你必须遵守

### A. 改代码前

1. 涉及 **自动平铺白名单、stack、标签栏、多显示器、新窗口平铺、托盘 Set Panel Stack、Win+Shift+F、构建输出** 时，**先阅读**上述文档 §1–§2，确保实现不违背主需求。
2. 与文档冲突时，**以用户最新明确指令为准**；若用户未澄清，在回复中说明冲突并询问，不要静默偏离 §1。

### B. 改代码后（涉及需求时）

3. 本次修复的 **BUG**、用户提出的 **建议**（若改变或澄清产品行为），**必须**更新 `Design/Requiment/FancyWM-主要需求与约定.md`：
   - BUG → §3 表格追加一行（日期、现象、原因/处理、相关文件）
   - 新主需求 → §1 增补条目
   - 已采纳建议 → §4 更新状态
4. 更新文档 **不替代** `pending_commit_notes.txt`：有代码/脚本改动时仍按 `pending-commit-notes.mdc` 写提交备注。
5. 仅问答、且未改仓库：不必改需求文档。

### C. 核心需求摘要（详见主文档）

- **Win+Shift**：仅快捷键列表提示；无捐赠/评价/等待操作 toast；异常提示保留
- **自动平铺白名单**：仅规则 Include 列表匹配才自动平铺；临时弹窗默认浮动原尺寸
- 托盘：非最大/最小窗口 **全部 stack**
- Stack：**右键拖标签** 排序；标签与 stack 一致
- **Win+Shift+F**：动态 stack（当前焦点布局）
- **双屏**：stack 状态跨屏一致；stack 模式下主窗口进 stack，**辅助弹窗**（查找/查询等）浮动原尺寸
- 构建：`latest` 全量、`latestmin` 增量 DLL
