---
description: 问题与解决记录文档——每次 BUG/需求澄清后须在 FancyWM-问题与解决记录.md 底部追加
alwaysApply: true
---

# 问题与解决记录（Issue Resolution Log）

主文档：**`Design/Requiment/FancyWM-问题与解决记录.md`**

与 `FancyWM-主要需求与约定.md` 分工：
- **主需求文档**：产品「要什么」（§1 需求、§3 简表）
- **问题与解决记录**：「出过什么事、用户要什么、最终怎么改的」（含错误尝试与回归点）

## 文档结构（固定）

文首依次为：**维护说明** → **索引** → **记录格式** → 正文条目（按时间正序，旧在上、**新在下**）。

## 你必须遵守

### A. 何时必须写入

完成以下任一情况后，**必须**在 `FancyWM-问题与解决记录.md` **文件最底部追加一节**（使用文内「记录格式」模板，前加 `---` 分隔）：

1. 用户报告的 **BUG** 或联调异常（含「改了但没效果」的迭代）
2. 用户**澄清**产品行为（与先前理解或实现不一致）
3. 用户提出的**实现方案**被采纳并完成代码修改
4. 同一问题多轮修复：每轮关键结论可合并为一节，须写明**最终**处理方式与曾走过的弯路

**禁止**在正文中间或文首插入新条目；**禁止**为「最新在上」而挪动已有条目。

### B. 每条记录须包含

- **现象**：用户看到什么
- **用户期望**：客户想怎样（尽量保留用户表述）
- **原因**：根因
- **最终处理**：改了哪些文件/函数/策略
- **备注**（可选）：回归测试步骤、勿再犯的误改

### C. 与其他文档的关系

1. 仍须按 `design-requirements.mdc` 更新 `FancyWM-主要需求与约定.md`（§1 需求、§3 BUG 简表）。
2. 问题与解决记录 **不替代** `pending_commit_notes.txt`；有代码改动时两者都要写。
3. 仅问答、未改仓库、且无新 BUG/澄清：**不必**改本文。
4. 主文档 §3 仅有简表、本文无详述时，可酌情更新文首**索引**表（索引仍在文首，不随条目下沉）。

### D. 改代码前

涉及 stack、多屏、Win+Shift+F、托盘 Set Panel Stack、白名单平铺时，除主需求文档外，**宜从本记录底部向上**浏览最近条目，避免重复已否定的方案（例如：取消 stack 用 split 拆壳、每窗独立 `WrapInStackPanel` stack 壳）。
